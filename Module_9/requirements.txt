# Module 9 Hands-on Projects - dependencies
# Install with:  pip install -r requirements.txt
#
# Project 3 (Career Toolkit) needs NONE of these - it uses the standard library.
# Project 1 needs: flask, scikit-learn, joblib
# Project 2 needs: streamlit, scikit-learn, pandas

flask>=3.0
streamlit>=1.30
scikit-learn>=1.3
joblib>=1.3
pandas>=2.0
